Lorenz attractor - физическая система, решающаяся через диффуры
(x.,y.,z.)

scipy.pde

class LorenzDinamics:
__init__(.., dict {params xyz}) | (step, interval [t0, t1], noize [sigma(x)], [sigma(y)], [sigma(z)], sigma, beta, ro, x0, y0, z0)

__call__(self, t, state) | state = (x, y, z) -> x.,y.,z.


class Simulator:
__init__(object, {params?})

run() -> Решение уравнения в цикле [t0, t1]. На каждый момент времени t должен return (x, y, z)


Нужно оформить решение в виде пакета.